from distutils.core import setup

setup(
    name = "pyrewrite",
    license = "MIT",
    description = "Python term rewriting",
    packages = ['rewrite',
                'rewrite.tests'],
    version = "dev",
)
